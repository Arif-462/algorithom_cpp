

1. Define state
    fun(n,x) = max(happiness til day n given that we are doing th task x on day n)

2.
    fun(n,x) = max(fun(n-1, y where x!= y) + c(h[n,x]) (day four max happiness))

3. base case define


Time complexit: O(n*x);


