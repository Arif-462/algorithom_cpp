#incldue<bits/stdc++.h>
using namespace std;

int main()
{
    Limitation's of dijkstra algorithm:
        1. Negative weight should not allow in the graph because not execute properly.
        2. Should not cycle in this graph.
    return 0;
}
